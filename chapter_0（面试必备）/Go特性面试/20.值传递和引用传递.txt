在 Go 中，值传递和引用传递的区别如下：

值传递：

当函数参数是基本数据类型（如 int、float、bool、string 等）时，传递的是值的副本，修改副本不会影响原始数据。
适用于数据量较小的类型，性能开销较低。
引用传递：

当函数参数是引用类型（如切片、映射、指针、通道等）时，传递的是引用，函数内部修改数据会影响原始数据。
适用于数据量较大的类型，避免复制带来的性能开销。
总结：

值传递：复制值，不影响原数据。
引用传递：传递地址，修改会影响原数据。