VRRP协议：
1.选择协议：
在众多路由中选择一个Master主路由，其余为Backup备份路由；
Master路由用来真实处理所有请求，定时发布状态信息给Backup路由
当Master路由宕机，所有Backup路由再次竞争出一个Master路由替换宕机的Master主路由

2.路由容错协议：
Master路由和Backup路由之间的心跳检测
当Backup路由没有按时收到Master路由的状态信息，就会产生替换Master路由

环境搭建：
2台nginx+keepalived服务器作为负载均衡器
3台tomcat服务器作为服务端

keepalived作用：
1.两台nginx服务器通过VRRP协议定出Master主路由和Backup副路由
2.Master主路由和Backup备份路由虚拟出一个虚拟的IP地址，让用户不用关注主副路由的区别，直接访问虚拟IP即可
3.虚拟IP收到请求，找到Master主路由处理请求，如果Master主路由宕机，Backup备份路由自动成为Master主路由

