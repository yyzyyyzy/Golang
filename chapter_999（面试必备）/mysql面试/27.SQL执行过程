第一步:客户端把语句发给服务器端执行
第二步:语句解析
    1. 查询高速缓存(library cache)
    2. 语句合法性检查(data dict cache)
    3. 语言含义检查(data dict cache)
    4. 获得对象解析锁(control structer)
    5. 数据访问权限的核对
    6.  确定最佳执行计划
第三步:语句执行
第四步:提取数据