一.基本数据类型：
整数型（int）
1.int8占用1字节 范围为-128~127
2.32位操作系统占用4个字节
3.64位操作系统占用8个字节

浮点型（float32/float64）
1.开发使用float64类型，保留精度

字符型（byte）
1.零值为0
2.字符型需要用byte来保存，由字符组成
3.字符类型（byte）与uint8类型等价
4.汉字占3个字节（字节翻译为byte）

字符串型
1.零值为""
2.单引号表示字符，双引号表示字符串
3.末尾有隐藏的\0，用于结束字符串


布尔型（bool）
1.零值为false

复数类型（complex64/complex128）
1.默认为complex128类型
2.使用内置函数取实部real(),取虚部imag()

二.复杂数据类型
1.函数
2.数组
3.切片（slice）
4.map
5.结构体
6.指针（pointer）
8.管道（channel）