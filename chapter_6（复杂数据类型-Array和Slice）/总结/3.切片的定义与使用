切片的结构：
1.分为三个部分，元素存哪里（默认零值nil），存了多少个元素（长度），可以存多少个元素（容量）
2.本质上就是数组，数组和切片由于不是固定长度和容量的，所以存放在堆区

切片的定义：
1.切片的零值定义：var slice []int  // data=nil || len=0 || cap=0
2.切片的自推导定义：slice := []int {1,2,3}  // data={1,2,3} || len=3 || cap=3
3.切片的make()定义：make([]int, 2, 5) // data=nil || len=2 || cap=5




