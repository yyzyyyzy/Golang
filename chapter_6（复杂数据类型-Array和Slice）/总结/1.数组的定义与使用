1.数组定义格式:
  var arr [n]type
  arr := [5]type{1,2,3,4,5}
  arr := [...]type{1,2,3,4,5} //自推导数组元素的个数
  arr := [5]type{2: 20, 4: 30} // 自定义数组下表对应的元素
  n表示数组长度，type表示数组存储类型。

2.数组的长度是该数组类型的一个内置常量，可以用Go语言的内置函数len()来获取
  arrLength := len(arr)

3.数组开辟的内存空间是连续的

4.由于数组长度牢固，内存一连