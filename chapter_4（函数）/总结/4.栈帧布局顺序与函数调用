①栈区：
一般用来存储基本数据类型;
②栈的数据结构：
先进后出,后进先出,只能在栈顶(允许元素插入与删除的一端称为栈顶)进行插入和删除操作;

③栈帧在虚拟内存空间布局顺序：
1.调用函数的栈基地址
2.调用函数的局部变量
3.调用函数的返回值
4.调用函数的参数
5.被调用函数的返回地址（被调用函数执行完毕会销毁以下栈帧并跳回此处）
6.被调用函数的局部变量
7.被调用函数的返回值
8.被调用函数的参数

ps：被调用函数的参数和返回值，都在调用者的函数栈帧中


